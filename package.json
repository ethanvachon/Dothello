{
  "name": "partner-checkpoint",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run setup:client && npm run setup:server && npm run build:client",
    "setup:client": "cd partner-checkpoint.client && npm i",
    "setup:server": "cd partner-checkpoint.server && npm i",
    "build:client": "npm run --prefix partner-checkpoint.client build",
    "start": "node partner-checkpoint.server/index.js"
  },
  "engines": {
    "node": "^14.15.4"
  }
}
